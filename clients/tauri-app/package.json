{
  "name": "amebaeath-tauri-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "parcel build src/index.html --dist-dir ../dist",
    "dev": "parcel src/index.html",
    "build:sidecar": "powershell -ExecutionPolicy Bypass -File ./scripts/build-sidecar.ps1"
  },
  "devDependencies": {
    "parcel": "^2.9.3"
  }
}
