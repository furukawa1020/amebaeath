name: CI (manual - disabled for automatic runs)

# Workflow has been intentionally switched to manual trigger per user request.
on:
  workflow_dispatch: {}
 
jobs:
  noop:
    runs-on: ubuntu-latest
    if: false
    steps:
      - name: noop
        run: echo "CI is intentionally disabled."
